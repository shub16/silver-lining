# This is a program of Tkinter with a text box.

import Tkinter 
from Tkinter import *
root =Tk()
root.title('Widget 1')
Label (text='enter the text').pack(side=TOP,padx=100,pady=10)
Entry(root, width=10).pack(side=TOP,padx=10,pady=10)
Button(root, text='Enter').pack()


root.mainloop()

